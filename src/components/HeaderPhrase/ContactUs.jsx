import React from 'react';
import '../../styles/hero/amenty.scss';

const ContactPhrase = () => {
    return (
        <div className="hero">
            <div className="hero__inner">
                <h1 className="hero__heading">
                    <span className="hero__heading--left"> We await your </span>
                    <span className="hero__heading--right">  mails and requests </span>
                </h1>
            </div>
        </div>
    )
}

export default  ContactPhrase
